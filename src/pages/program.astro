---
import Layout from '../layouts/Layout.astro';

const programs = [
  {
    category: 'Fikih',
    levels: [
      { name: 'Pemula', book: 'Kitab Safinatunnajah' },
      { name: 'Mampu', book: 'Kitab Fathu al-Qarib' },
      { name: 'Cakap', book: 'Kitab Minhaj al-Talibin' },
    ],
  },
  {
    category: 'Aqidah',
    levels: [
      { name: 'Pemula', book: 'Kitab Aqidat al-Awam' },
      { name: 'Mampu', book: 'Kitab Maqalat al-Islamiyyin' },
      { name: 'Cakap', book: 'Kitab Al-qawl al-Sadid' },
    ],
  },
  {
    category: 'Tajwid',
    levels: [],
  },
];

const recordings = ['Fikih', 'Aqidah', 'Tajwid'];
---
<Layout>
  <div class="relative isolate overflow-hidden bg-gray-900">
    <div class="mx-auto max-w-7xl px-6 lg:px-8 py-24 sm:py-32">
      <div class="text-center">
        <h1 class="text-4xl font-bold tracking-tight text-white sm:text-6xl">Program Pembelajaran</h1>
        <p class="mt-6 text-lg leading-8 text-gray-300">
          Pilih program yang sesuai dengan minat dan tingkat pemahaman Anda.
        </p>
      </div>
    </div>
  </div>

  <div class="bg-white py-24 sm:py-32">
    <div class="mx-auto max-w-7xl px-6 lg:px-8">
      <div class="mx-auto max-w-2xl lg:text-center">
        <h2 class="text-base font-semibold leading-7 text-blue-600">Program Interaktif</h2>
        <p class="mt-2 text-3xl font-bold tracking-tight text-gray-900 sm:text-4xl">Pilih Program yang Sesuai untuk Anda</p>
        <p class="mt-6 text-lg leading-8 text-gray-600">
          Kami menyediakan berbagai program pembelajaran kitab-kitab turoth dalam berbagai tingkatan.
        </p>
      </div>

      <div class="mx-auto mt-16 grid max-w-2xl grid-cols-1 gap-8 sm:grid-cols-2 lg:mx-0 lg:max-w-none lg:grid-cols-3">
        {programs.map(program => (
          <div class="flex flex-col justify-between rounded-2xl bg-gradient-to-br from-blue-50 to-blue-100 p-8 shadow-xl transition-transform duration-300 hover:scale-105">
            <div>
              <h3 class="text-2xl font-bold text-blue-900">{program.category}</h3>
              <div class="mt-6 flex flex-auto flex-col text-base leading-7 text-gray-700">
                {program.levels.length > 0 ? (
                  <ul class="space-y-4">
                    {program.levels.map(level => (
                      <li class="flex items-start">
                        <div class="flex-shrink-0">
                          <svg class="h-6 w-6 text-blue-600" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
                          </svg>
                        </div>
                        <p class="ml-3">
                          <span class="font-semibold">{level.name}:</span> {level.book}
                        </p>
                      </li>
                    ))}
                  </ul>
                ) : (
                  <p class="flex-auto italic">Program tajwid tersedia dalam bentuk rekaman.</p>
                )}
              </div>
            </div>
          </div>
        ))}
      </div>

      <div class="mx-auto mt-24 max-w-2xl lg:text-center">
        <h2 class="text-base font-semibold leading-7 text-blue-600">Rekaman Kajian</h2>
        <p class="mt-2 text-3xl font-bold tracking-tight text-gray-900 sm:text-4xl">Akses Rekaman Kapan Saja</p>
        <p class="mt-6 text-lg leading-8 text-gray-600">
          Tidak sempat mengikuti kelas langsung? Kami menyediakan rekaman kajian yang bisa Anda akses kapan saja.
        </p>
        <div class="mt-10">
          <div class="flex items-center justify-center gap-x-6">
            {recordings.map(rec => (
              <span class="inline-flex items-center rounded-full bg-blue-100 px-4 py-2 text-sm font-medium text-blue-800 ring-1 ring-inset ring-blue-600/20">{rec}</span>
            ))}
          </div>
        </div>
      </div>
    </div>
  </div>
</Layout>
