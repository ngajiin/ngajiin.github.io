---
import Layout from '../layouts/Layout.astro';

const programs = [
  {
    category: 'Fikih',
    levels: [
      { name: 'Pemula', book: 'Kitab Safinatunnajah' },
      { name: 'Mampu', book: 'Kitab Fathu al-Qarib' },
      { name: 'Cakap', book: 'Kitab Minhaj al-Talibin' },
    ],
  },
  {
    category: 'Aqidah',
    levels: [
      { name: 'Pemula', book: 'Kitab Aqidat al-Awam' },
      { name: 'Mampu', book: 'Kitab Maqalat al-Islamiyyin' },
      { name: 'Cakap', book: 'Kitab Al-qawl al-Sadid' },
    ],
  },
  {
    category: 'Tajwid',
    levels: [],
  },
];

const recordings = ['Fikih', 'Aqidah', 'Tajwid'];
---
<Layout>
  <div class="relative isolate overflow-hidden bg-gray-900">
    <div class="mx-auto max-w-7xl px-6 lg:px-8 py-24 sm:py-32">
      <div class="text-center">
        <h1 class="text-4xl font-bold tracking-tight text-white sm:text-6xl">Program Pembelajaran</h1>
        <p class="mt-6 text-lg leading-8 text-gray-300">
          Pilih program yang sesuai dengan minat dan tingkat pemahaman Anda. Kami menyediakan berbagai program pembelajaran kitab-kitab turoth dalam berbagai tingkatan.
        </p>
      </div>
    </div>
  </div>

  <div class="bg-white py-24 sm:py-32">
    <div class="mx-auto max-w-7xl px-6 lg:px-8">
      {/* --- START OF MODIFIED SECTION --- */}
      <div class="mx-auto grid max-w-2xl grid-cols-1 gap-8 sm:grid-cols-2 lg:mx-0 lg:max-w-none lg:grid-cols-3">
        {programs.map(program => (
          <div class="group relative flex flex-col rounded-2xl border border-gray-200 bg-white p-8 transition-shadow duration-300 hover:shadow-2xl">
            {/* Ring effect on hover */}
            <div class="absolute inset-0 rounded-2xl ring-1 ring-inset ring-transparent transition-all duration-300 group-hover:ring-blue-500"></div>
            
            <div class="flex-grow">
              <h3 class="text-2xl font-bold text-gray-900">{program.category}</h3>
              <div class="mt-6 flex flex-col text-base leading-7 text-gray-700">
                {program.levels.length > 0 ? (
                  <ul class="space-y-4">
                    {program.levels.map(level => (
                      <li class="flex items-start">
                        <div class="flex-shrink-0 pt-1">
                          <svg class="h-5 w-5 text-blue-600" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor" aria-hidden="true">
                            <path fill-rule="evenodd" d="M16.704 4.153a.75.75 0 01.143 1.052l-8 10.5a.75.75 0 01-1.127.075l-4.5-4.5a.75.75 0 011.06-1.06l3.894 3.893 7.48-9.817a.75.75 0 011.052-.143z" clip-rule="evenodd" />
                          </svg>
                        </div>
                        <p class="ml-3">
                          <span class="font-semibold text-gray-800">{level.name}:</span> {level.book}
                        </p>
                      </li>
                    ))}
                  </ul>
                ) : (
                  <p class="flex-auto italic text-gray-500">Program tajwid tersedia dalam bentuk rekaman.</p>
                )}
              </div>
            </div>
            
            <div class="mt-8">
              <a href="#" class="text-sm font-semibold leading-6 text-blue-600">
                Lihat Detail <span aria-hidden="true">→</span>
              </a>
            </div>
          </div>
        ))}
      </div>
      {/* --- END OF MODIFIED SECTION --- */}

      <div class="mx-auto mt-24 max-w-2xl lg:text-center">
        <h2 class="text-base font-semibold leading-7 text-blue-600">Rekaman Kajian</h2>
        <p class="mt-2 text-3xl font-bold tracking-tight text-gray-900 sm:text-4xl">Akses Rekaman Kapan Saja</p>
        <p class="mt-6 text-lg leading-8 text-gray-600">
          Tidak sempat mengikuti kelas langsung? Kami menyediakan rekaman kajian yang bisa Anda akses kapan saja.
        </p>
        <div class="mt-10">
          <div class="flex items-center justify-center gap-x-6">
            {recordings.map(rec => (
              <span class="inline-flex items-center rounded-full bg-blue-100 px-4 py-2 text-sm font-medium text-blue-800 ring-1 ring-inset ring-blue-600/20">{rec}</span>
            ))}
          </div>
        </div>
      </div>
    </div>
  </div>
</Layout>